# --- Core ------------------------------------------------------------------
# 5.2 is the current LTS. The lower bound stays at 5.2 rather than 6.0 so the
# project installs on Python 3.11 as well as 3.12+; nothing in the codebase
# uses a 6.x-only API.
Django>=5.2,<7.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
psycopg2-binary>=2.9
python-dotenv>=1.0
django-cors-headers>=4.4
Pillow>=10.0

# --- Real-time (Whispers, live notifications) -------------------------------
# Channels gives us ASGI + WebSockets. channels-redis is only *used* when
# REDIS_URL is set; without it the in-memory channel layer is used, so local
# development needs no Redis at all. See config/settings.py.
channels>=4.1
channels-redis>=4.2
daphne>=4.1

# --- Storage ---------------------------------------------------------------
# Local FileField storage is the default. Setting USE_S3=True swaps in an
# S3-compatible backend with no code changes — see config/settings.py.
django-storages>=1.14
boto3>=1.34

# --- Production serving -----------------------------------------------------
gunicorn>=22.0
uvicorn[standard]>=0.30
whitenoise>=6.7

# --- Optional production add-ons (activated by env vars only) ---------------
# sentry-sdk is imported lazily; without SENTRY_DSN set it is never used.
sentry-sdk>=2.0
django-filter>=24.0
